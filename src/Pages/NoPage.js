import React from "react";
// Error page if no page is found via navbar

const NoPage = () => {
    return (
        <div>
            <h2>Error 404: Not found</h2>
        </div>
    );
};

export default NoPage;